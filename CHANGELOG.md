# Changelog

## Unreleased

## 2.3.0-alpha.1 - 2026-09-25

本版本为预发布版本，集中更新自适应工作区、日期与时间选择、设置与编辑体验，并修复通知和数据保存问题。建议体验前先导出完整应用备份。

### 新增

- 新增“功能管理”，可分别启用或停用学生课表与通用日程，至少保留一个工作区；停用时保留数据与配置，并停止对应功能和提醒，之后可重新启用。
- 通用日程新增 1–14 天的自定义日期范围，支持跨周、跨月选择、直接输入起止日期和手机拖选；保存后可按所选天数前后浏览，重启后保留范围设置。
- 自定义日程视图新增自动／手动最小列宽；空间充足时等宽铺满，不足时横向滚动，日期表头、全天栏与时间网格保持对齐。
- 新增“设置 → 关于 → 接收预发布更新”开关，默认关闭；开启后可接收 Alpha、Beta、RC，偏好会持久保存，切换渠道时清理旧的更新提示。

- 新增 Microsoft Store 专用 MSIX 构建与产品身份配置，商店包使用独立数字版本规则并检查包清单；商店版由 Microsoft Store 管理更新，不使用 GitHub 的预发布开关。商店上架仍需单独送审。

### 工作区与界面

- 重构桌面与宽平板布局，以资源侧栏、课表／日历主区域和详情／编辑面板协同工作；窗口变窄时自动调整为浮层或独立任务，保留浏览位置、面板状态与编辑草稿。
- 仅启用一个工作区时不再显示多余的模式切换；双工作区导航保持同等层级，侧栏展开与收起时不再造成图标跳动或日历突变重排。
- Windows 顶部操作与窗口控制整合为一行，改善窄窗口与大字体下的日期、跳周和更多菜单布局；修复首次启动页、空课表主页无法拖动窗口的问题。
- 修复手机整周日程布局，恢复课程／日程详情和编辑的底部弹窗；首页顶部操作合并为一行，日期标记与课表统一，减少不必要的空白。
- 优化手机底部工作区导航的选中状态、图标位置与系统安全区，统一导航区背景，避免重复底部留白。
- 紧凑月视图改为上方月历、下方当天安排的单一滚动页面，缩减空日程占位；改善农历、大字体和两位数日期的显示，宽屏继续保留侧边安排。
- 优化日／周视图的固定表头、全天日程展开、重叠事件分栏与溢出入口，短事件不再为适应布局而虚增显示时长。
- 统一主页、侧栏、标题栏、详情面板和弹窗的背景与层级，改善浅色／深色模式的一致性，同时保留用户选择的主题色、课程色与分类色。

### 日期与时间选择

- 统一日期选择器在日、周、月导航和业务表单中的体验，支持日历与输入模式；手机按任务使用紧凑弹窗、锚定跳周菜单或底部日期范围选择，桌面选择器就近锚定操作入口。
- 时间选择改为独立循环的小时／分钟滚轮，并保留直接输入、24 小时格式与输入校验；窄屏、键盘弹出和大字体下的操作区可自适应换行。
- 课表跳周改为紧凑数字网格，打开时显示当前选中周，支持滚动和键盘操作，并区分实际当前周与学期外的边界周。
- 统一日期选中高亮和数字居中，修复日期被背景裁切、月份／年份层过宽及重复标题占位；切换输入模式、旋转或调整窗口大小时保留选择草稿。

### 设置、编辑与数据管理

- 设置首页改为分组总览，优先展示已启用的课表／日程设置，之后为通知、外观与语言、功能管理、数据与隐私和关于；宽屏索引定位同一组内容，详细选项收进独立页面。
- 设置、主题与提醒控件统一为连接式分组，调整图标、文字、当前值和说明层级；选项菜单锚定当前值，修复菜单关闭后的焦点及图标居中问题。
- 外观页可编辑另一工作区的主题而不切换当前工作区；语言设置保留独立搜索页和搜索状态，保存期间统一保护工具栏与系统返回。
- 重做节次时间集编辑器，按时间顺序展示节次、起止时间、时长与课间；支持紧凑新增入口、时间校验和清晰的等待／保存／失败状态，增删与调整窗口时保留列表位置。
- 分类管理统一为可滚动列表，展示颜色、事件数量、显隐与操作；新增和重命名使用简洁确认弹窗，取消不创建占位分类，保存失败保留草稿。
- 课程专属提醒收进“更多”，摘要区分沿用默认、关闭和自定义；无效的自定义提前分钟数会明确提示，不再静默退回默认设置。
- 统一课表／日程导入导出页的分组、间距与操作布局，窄屏和大字体时改单栏；移除空课表重复标题、宽屏设置重复返回按钮和“数据与隐私”中的重复备份入口。
- 完整应用备份与恢复在设置总览保留唯一入口，并保留工作区启用状态、自定义日期范围和列宽等设置；修复存储恢复提示出现时的标题栏对齐。

### 修复与稳定性

- 加固编辑器退出和草稿保护，完整数据替换后清退旧编辑会话；导入提交与保存期间阻止重复操作或返回，失败后保留可重试内容。
- 校验日程结束时间及节次输入，明确展示错误，避免无效时间被静默修正后保存。
- 修复通知在重建排程、切换语言、重启或恢复后可能重复投递的问题；仅对确认未提交且仍在补发时限内的提醒补发一次，保留“稍后提醒”的独立状态。
- 修复通知运行时锁顺序与队列清理引发的死锁，避免相关导航和保存操作卡住；完善 Windows 待处理通知识别与重复排程清理。
- 更新比较遵循 SemVer：正确识别 `alpha.1 < alpha.2 < beta.1 < rc.1 < 正式版`，数字后缀按数值比较，忽略 `+构建号`；同版本 RC 升正式版现在会提示更新。
- GitHub 更新检查按渠道筛选全部分页中的有效发布，不再遗漏预发布版本或误用发布时间作为版本顺序；支持自定义更新源提供正式版／预发布列表，草稿发布始终排除。
- 更新检测与版本显示优先读取 Flutter 的完整构建版本，避免 iOS/macOS 原生版本字段丢失预发布后缀；Windows MSIX 打包兼容预发布版本，并使用数字版本生成包身份。

### 开发者预览

- 新增默认关闭的 AI 侧栏布局预览，仅显示只读工作区上下文并保留本次消息草稿，不会发送请求、执行操作或修改数据；这不是已接入的 AI 对话功能。

### 预发布说明

- 支持更新渠道的新版本默认只提示正式版；体验本预发布版本需开启预发布更新或手动安装。旧客户端仍使用旧更新逻辑，不会因发布此版本自动获得新能力。
- 按 SemVer，`2.3.0-alpha.1` 低于 `2.3.0`；版本号已经是 `2.3.0` 的安装包不会把本版本识别为升级。应用版本比较与 Android／Windows 的构建号安装规则相互独立。

构建号：14（`2.3.0-alpha.1+14`）。

## 2.2.0 - 2026-09-01

### 新增

- 新增系统通知，支持课程与日程提醒、通知操作及点击跳转（Android、Windows）。
- 新增提醒与通知设置、课程提醒继承/关闭/自定义和日程新建默认提醒。

### 修复

- 修复部分提醒在应用重启、快速修改或跨时区后可能重复、遗漏或无法正确操作的问题。

## 2.1.1 - 2026-08-31

### 改进

- 课表与日程的顶部工具栏导航现在支持排序、隐藏和收进更多菜单。
- 优化日程视图的全天日程展示，支持折叠全天栏并修复更多提示范围。
- 学校网页导入的内置浏览器优化为更接近日常浏览器的操作。
- 优化 AI 解析导入流程。
- 优化 Windows 数据存储迁移和首次进入时的工作区导航体验。
- 增加一键清除 Sked 数据的功能。
- 优化 AI API 配置入口及关于页外部链接。

### 修复

- 修复网页导入预览中不可用的选项看起来仍可点击的问题；窄屏或放大字体时，底部操作按钮改为纵向排列，避免按钮文字被挤压。
- 修复在学校网页内点击页内锚点链接后返回，导入按钮会永久失效、顶部进度条一直转的问题。
- 修复弹出的学校登录窗口在页面加载前显示上一个站点网址的问题。

## 2.1.0 - 2026-08-16

### 改进

- 优化通用日程顶部导航：视图按钮支持顺序循环或菜单选择，日期入口更紧凑并可长按回到今天；新增工具栏空间分配预设，日期文本会按可用宽度渐进压缩。
- 通用日程日、周视图支持自定义每小时网格高度，短事件在紧凑高度下仍可清晰点击。
- 通用日程分类管理与日程详情操作更加紧凑，所有未隐藏分类会在首页共同显示。
- 简化首次启动流程：选择起始工作区即表示已阅读并同意隐私政策，不再显示单独的隐私确认页。
- 新用户首次启动默认使用浅色主题；优化首次启动模式选择、课程与日程编辑界面以及节次时间集选择布局，课程描边改为独立页面，日程重复与提醒改为弹窗配置，减少重复提示和不必要的垂直占用。
- 学生课表与通用日程现在使用同等突出的自适应一级导航，并在切换工作区时保留当前浏览位置和上下文。
- 主界面工作区导航现在可在手机、平板和桌面窗口中统一隐藏；隐藏后可从工作区右上角进入设置。
- 大屏窗口中的完整工作区导航现在可折叠为紧凑侧栏，并会记住用户选择。
- 设置页现在同时显示课表与通用日程设置并按用途分组；主题页可直接切换工作区，设置首页会同时概览两套主题；宽屏主题设置布局更加紧凑，并新增红色预设。
- 主要工作区的视图、周次和日期导航现在使用更连贯的形状移动与方向过渡，并遵循系统的动效设置。
- 学生课表工作区现在支持窄屏日视图、日期条和可滚动时间轴；课表切换、周次导航及空课表操作在不同窗口尺寸下更清晰可达。
- 通用日程现在用统一工具栏浏览日、周、月和列表视图，日期切换与添加日程入口在手机和宽屏上保持一致。
- 原生平台的本地数据现在保存在操作系统管理的应用支持目录，不再写入用户“文档 / Documents”目录；旧版本留在该目录的文件不会被自动读取或迁移，升级前请先导出完整应用备份。
- 保存设置、课程、日程和导入内容时，现在会显示进度，并在失败后保留当前操作以便重试。
- 学校网页登录和明文 HTTP 课表解析现在会在发送数据前显示目标与风险确认。
- 网页课表导入现在只向解析服务提供站点来源，不再发送页面路径、查询参数或片段，并会在网页内容被截断时提示。
- 应用启动不再自动联网获取隐私版本或检查更新；更新检查仍可在设置中手动触发。
- 移除阿拉伯语界面；旧存档中的阿拉伯语设置会回退为英语，避免启动后界面方向反转。
- 优化学生课表页面布局：压缩视图、周次和日期导航，并新增日期条、周课表列宽度及滑动切换周数设置。
- 学生课表为空时也可从首页右上角进入设置。
- 课表与通用日程现在可分别隐藏主界面右下角的悬浮添加按钮，并优化空状态操作、导入菜单和节次时间集选择界面。
- 课表与通用日程现在可分别关闭长按空白网格添加内容的快捷操作；日程设置适用于日视图和周视图。
- 新建课表现在会先填写名称、学期与节次信息；课程编辑使用合并的时间范围控件，大屏设置页支持双列分组和整页滚动。

### 修复

- 修复保存失败时部分页面仍会关闭、丢失编辑草稿或显示与本地数据不一致的问题。
- 修复多个应用窗口或浏览器标签页同时打开时可能互相覆盖本地数据的问题；现在只允许一个实例写入。
- 修复解析服务返回超大、损坏、重定向或超时响应时可能继续等待或使用不完整结果的问题。
- 修复学校网页加载失败或超时后仍可能导入未完整页面的问题。
- 修复异常学校网页可能放大导入结果并导致应用卡顿或退出的问题。
- 修复快速重复操作网页登录安全确认时可能意外退出导入页面的问题。
- 修复升级后已同意较新隐私政策的用户可能被重复要求确认的问题。
- 修复保存过程中继续编辑时，新内容可能被较早的保存结果或失败回滚覆盖的问题。
- 修复本地数据、备份或学校站点数据无法读取时可能被后续写入覆盖的问题；现在会阻止自动写入，并在可读取时先保留恢复副本。
- 修复完整应用备份遗漏学校站点的问题，并支持在恢复中断后继续完成恢复。
- 修复完整备份恢复中断后可能重复覆盖已恢复数据或过早清除密钥的问题；恢复现在会分阶段继续，并在完成后安全清理记录。
- 修复夏令时切换地区的日期、周次、重复事件和全天事件可能偏移的问题。
- 修复带 UTC 时间的定时事件在日、周、月和列表视图中可能归到错误日期的问题。
- 修复未设置提醒的日程可能误出现在提醒条或提醒处理按钮中的问题。
- 修复提醒状态不会随时间自动刷新，以及旧版删除和已处理状态在夏令时迁移后可能丢失的问题。

## 2.0.1 - 2026-06-24

### 改进

- 优化通用日程切换日期、滑动视图和日历视图加载时的响应速度。
- 优化学生课表翻周和课程布局刷新时的界面流畅度。
- 减少部分日程和课表操作触发的重复刷新与本地数据写入。

## 2.0.0 - 2026-06-18

Sked 2.0.0 正式版带来了完整的通用日程能力，并继续强化学生课表、导入导出、隐私控制和整体使用体验。

### 新增

- 新增通用日程模式，可在学生课表和日程管理之间切换。
- 新增日、周、月、列表视图，支持按日期浏览和管理事件。
- 新增多日历管理，可创建、切换、重命名、隐藏和删除日历。
- 新增事件重复规则、跨天事件、全天事件和应用内提醒。
- 新增通用日程 JSON / ICS 导入导出，可导入日历文件或分享选中的日历。
- 新增完整应用备份与恢复，覆盖课表、日程、节次时间、学校站点和应用设置。
- 新增首次启动模式选择和隐私政策确认流程。
- 新增自定义 OpenAI 兼容课表解析配置，支持文本、HTML 和学校网页内容解析导入。

### 改进

- 全面更新为 Material 3 Expressive 界面风格，优化弹窗、底部面板、设置页和主要工作流。
- 改进主题设置，支持通用日程和学生课表下的主题色与多彩界面配置。
- 改进学生课表首页结构、课程卡片显示、冲突课程显示和当前/下一节课程提示。
- 改进通用日程时间线、月视图、事件卡片、日历颜色和农历显示效果。
- 改进导入预览流程，可在保存前确认解析结果、节次时间集和导入方式。
- 改进更新检查入口，统一使用 GitHub Releases。
- 提升通用日程切换日期、滑动视图和学生课表翻周时的响应速度。

### 修复

- 增强本地数据读取、备份恢复和异常数据处理的稳定性。
- 增强 JSON、ICS、学校站点和课表解析结果的校验与容错。
- 修复快速重复点击可能导致的重复弹窗、重复导入或重复操作。
- 修复部分日程提醒、重复事件、导入事件和日历颜色显示异常。
- 修复多语言文案、解析接口地址校验和设置页显示问题。
